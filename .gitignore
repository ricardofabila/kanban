# SQLite database and its journals — never commit user data
/data/*.sqlite
/data/*.sqlite-journal
/data/*.sqlite-wal
/data/*.sqlite-shm

# Backup snapshots are database copies — same reason
/data/backups/

.DS_Store
